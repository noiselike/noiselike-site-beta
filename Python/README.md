# run locally
```shell
# install python
brew install python
echo "alias python=python3" >> ~/.zshrc
# install flask
pip install Flask
# start website
python website.py
# access website
http://127.0.0.1:5000
```
